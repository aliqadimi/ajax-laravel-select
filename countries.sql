CREATE TABLE countries(
   id          INTEGER  NOT NULL PRIMARY KEY 
  ,name        VARCHAR(17) NOT NULL
  ,slug        VARCHAR(26)
   );
 INSERT INTO countries(id,name,slug) VALUES (1,'ایران','ایران');